Algoritmo "tabuada"

Var

   valor, resultado, tabuada : Inteiro

Inicio

      Escreva ("Deseja a tabuada para qual valor? ")
      Leia (valor)
      
      resultado <- 0
      tabuada <- 0
      
      Para tabuada de 1 ate 10 faca
           resultado <- valor * tabuada
           EscrevaL (valor, " X",tabuada, " =",resultado)
      FimPara

Fimalgoritmo